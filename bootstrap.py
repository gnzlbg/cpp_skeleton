# Modifications performed by the bootstrap script:

# Variables needed: project_name, github_user

# In /CMakeLists.txt
# - replace "project_name" with your project's name within this file
# - replace "github_user" with your github's user name

# In /test/CMakeLists.txt
# - replace "project_name" with your project's name within this file

# capitalize project_name

# - In the `CMakeLists.txt` file:
#   - name of the project
# - name of the `include/project_name` directory and `project_name.hpp` files
#   - using git mv  
# - formatting style in the `.clang-format` file
